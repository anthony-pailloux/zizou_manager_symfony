{% extends 'base.html.twig' %}
{% block body %}
{{ team.name }}
<form action="{{ path('team_delete', { id: team.id }) }}" method="post"><input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team.id) }}"><button type="submit">suppr</button></form>
{% endblock %}
